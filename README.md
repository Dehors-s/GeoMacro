# GeoMacro MVP

GeoMacro captures ArcGIS Pro geoprocessing history and generates repeatable Python scripts.

## Current scope

- Normalize geoprocessing history into a stable event model.
- Extract variable parameters from input and output paths.
- Generate linear ArcPy scripts for whitelist tools.
- Validate generated script syntax and obvious path issues.
