<UserControl x:Class="GeoMacroAddin.Dockpane.GeoMacroPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500"
             d:DesignWidth="450">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- ======== Title ======== -->
        <TextBlock Grid.Row="0"
                   Text="GeoMacro Pipeline"
                   FontSize="15"
                   FontWeight="Bold"
                   Margin="0,0,0,6" />

        <!-- ======== Toolbar ======== -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,4">
            <Button Content="⟳ Refresh" Command="{Binding RefreshHistoryCommand}"
                    Padding="8,2" Margin="0,0,4,0" />
            <Button Content="All" Command="{Binding SelectAllCommand}"
                    Padding="6,2" Margin="0,0,4,0" />
            <Button Content="None" Command="{Binding DeselectAllCommand}"
                    Padding="6,2" Margin="0,0,8,0" />
            <Button Content="⚡ Generate" Command="{Binding GenerateScriptCommand}"
                    Padding="10,2" FontWeight="SemiBold"
                    IsEnabled="{Binding IsGenerating, Converter={x:Null}}" />
        </StackPanel>

        <!-- ======== Event List ======== -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,0,0,4">
            <ItemsControl ItemsSource="{Binding Events}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="8" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <CheckBox Grid.Column="0"
                                      IsChecked="{Binding IsSelected}"
                                      VerticalAlignment="Center" />

                            <Border Grid.Column="2"
                                    Background="SteelBlue"
                                    CornerRadius="3"
                                    Padding="4,1">
                                <TextBlock Text="{Binding Badge}"
                                           FontSize="10"
                                           Foreground="White" />
                            </Border>

                            <TextBlock Grid.Column="3"
                                       Text="{Binding SuccessIcon}"
                                       FontSize="12"
                                       Margin="4,0"
                                       VerticalAlignment="Center" />

                            <StackPanel Grid.Column="4" Orientation="Vertical">
                                <TextBlock Text="{Binding DisplayName}"
                                           FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{Binding ToolPath}"
                                           FontSize="10"
                                           Foreground="Gray"
                                           TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- ======== Script Preview ======== -->
        <Border Grid.Row="2" BorderBrush="LightGray" BorderThickness="1"
                Margin="0,130,0,0" CornerRadius="3" Padding="4">
            <TextBox Text="{Binding ScriptPreview, Mode=OneWay}"
                     IsReadOnly="True"
                     FontFamily="Consolas"
                     FontSize="11"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     Background="#F5F5F5"
                     TextWrapping="NoWrap" />
        </Border>

        <!-- ======== Bottom Bar ======== -->
        <Grid Grid.Row="3" Margin="0,4,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="{Binding StatusText}"
                       FontSize="11"
                       Foreground="DimGray"
                       VerticalAlignment="Center"
                       TextTrimming="CharacterEllipsis" />

            <Button Grid.Column="1"
                    Content="💾 Save Script"
                    Command="{Binding SaveScriptCommand}"
                    Padding="10,3"
                    Margin="4,0,0,0" />
        </Grid>
    </Grid>
</UserControl>
